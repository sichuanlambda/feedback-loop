<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Score Your "Viral" Tweet</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Optional: jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <!-- Optional: Popper JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <!-- Bootstrap JS -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <style>
        .gpt-form-container {
            margin-top: 2rem;
            margin-bottom: 2rem;
        }
        .gpt-answer-container {
            margin-top: 1rem;
            background-color: #f8f9fa;
            padding: 1rem;
            border-radius: 5px;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Feedback App</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="http://localhost:3000/feedbacks/dashboard">Dashboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="http://localhost:3000/feedbacks/new">Submit Feedback</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="http://localhost:3000/about">About</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="http://localhost:3000/roastery">Roastery 🔥</a>
            </li>
        </ul>
    </div>
</nav>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="text-center">
                <h1 class="mt-5">Welcome to the Roastery</h1>
                <p>Paste in your supposedly "viral" tweet, and we'll see just how true that is...</p>
            </div>

            <div class="gpt-form-container">
                <%= form_with url: ask_gpt_path, method: :post, id: "gpt-form", local: false do |form| %>
                    <div class="form-group">
                        <%= text_area_tag :question, nil, placeholder: "Wen lambo to the moon 🚀🤔", class: "form-control", rows: 4 %>
                    </div>
                    <%= submit_tag "Check it", class: "btn btn-primary btn-block" %>
                <% end %>
            </div>

            <div id="gpt-answer" class="gpt-answer-container"></div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const form = document.getElementById('gpt-form');
  form.addEventListener('submit', (event) => {
    event.preventDefault();

    const formData = new FormData(form);
    let question = formData.get('question');

    // Augment the user's input here
    const additionalContent = " Roast my tweet about its virality potential using gen z slang, unless the tweet has relative viral potential, the response should more make fun of the tweet in gen z slang";
    question += additionalContent;

    fetch('/ask_gpt', {
      method: 'POST',
      headers: {
        'Accept': 'application/json', 
        'X-CSRF-Token': document.querySelector("[name='csrf-token']").content,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({ prompt: question })
    })
    .then(response => response.json())
    .then(data => {
      console.log("Received data:", data); // Log the data for debugging
      if (data.text) {
        document.getElementById('gpt-answer').textContent = data.text;
      } else {
        document.getElementById('gpt-answer').textContent = 'No response or error received';
      }
    })
    .catch(error => {
      console.error('Error:', error);
      document.getElementById('gpt-answer').textContent = 'Error: Unable to fetch response.';
    });
  });
});
</script>

</html>