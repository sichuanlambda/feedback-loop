<div class="admin-place-edit">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Edit Place: <%= @place.name %></h1>
    <div>
      <%= link_to 'View Place', place_path(@place), class: 'btn btn-outline-primary', target: '_blank' %>
      <%= link_to 'Back to Places', admin_places_path, class: 'btn btn-secondary' %>
    </div>
  </div>

  <%= render 'form' %>
</div> 